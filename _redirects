# Netlify Redirects File
# https://docs.netlify.com/routing/redirects/

# Redirect form submissions to success page (optional)
/success /index.html?success=true 200

# Redirect www to non-www (or vice versa) - Uncomment and configure after adding custom domain
# https://www.yourdomain.com/* https://yourdomain.com/:splat 301!
# OR
# https://yourdomain.com/* https://www.yourdomain.com/:splat 301!

# Redirect old paths to new paths (example)
# /old-page /new-page 301
# /blog/old-post /blog/new-post 301

# Redirect external links (example)
# /instagram https://instagram.com/your-brand 302
# /twitter https://twitter.com/your-brand 302
# /tiktok https://tiktok.com/@your-brand 302

# 404 page (will use index.html as fallback)
/* /index.html 200

# API proxy example (if you add external API calls later)
# /api/* https://your-api.com/:splat 200

# A/B Testing example (Netlify Edge)
# /new-feature /variant-a 50
# /new-feature /variant-b 50

# Country/language redirects example
# / /en 302 Country=us
# / /es 302 Country=mx
# / /fr 302 Country=fr

# Role-based redirects (requires Netlify Identity)
# /admin/* /login 401! Role=!admin

# Prevent access to sensitive files
/.git/* /404 404
/.env* /404 404
/config/* /404 404

# Pretty URLs (optional - redirect .html to clean URLs)
# /about.html /about 301
# /contact.html /contact 301

# Force HTTPS (Netlify does this automatically, but can be explicit)
# http://* https://:splat 301!
